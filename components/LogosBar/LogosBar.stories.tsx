import { Meta, StoryObj } from '@storybook/react'

import LogosBar from './LogosBar'

const meta: Meta<typeof LogosBar> = {
  component: LogosBar,
}

export default meta
type Story = StoryObj<typeof LogosBar>

export const Default: Story = {}

import { Meta, StoryObj } from '@storybook/react'

import SpecializationCards from './SpecializationCards'

const meta: Meta<typeof SpecializationCards> = {
  component: SpecializationCards,
}

export default meta
type Story = StoryObj<typeof SpecializationCards>

export const Default: Story = {}
